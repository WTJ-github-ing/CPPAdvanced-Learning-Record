// 文件名：44
// 创建时间：2022/11/23 15:13:50
#include<iostream>
using namespace std;
#include<list>
#include<algorithm>

/*
list:将数据进行链式存储，迭代器是前移和后移，双向迭代器。
	是一种物理存储单元上非连续的存储结构，数据元素的逻辑顺序是通过链表中的指针链接实现的。

	链表由一系列节点组成。
		节点组成：一个是存储数据元素的数据域val，另一个是存储下一个节点地址的指针域next。

		stl中的链表是一个双向循环链表。

list优点：
	对任意的位置快速插入、删除。只用更改指向地址。
	动态存储分配，不会造成内存浪费和溢出。
缺点：遍历速度比数组慢。占用的空间比数组大。
*/

int main()
{

	system("pause");
	return 0;
}
